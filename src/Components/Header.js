import React from "react"

import './style.css'
function Header(){
    return(
        <div>
            <header className="navbar">This is the header</header>
        </div>
    );
}

export default Header